const utils = function (logger) {

    [1, 2, 3, 4].map(function sumIdx(v) {
        return i => {
            return v + i;
        };
    }).map(function (v) {
        return i => {
            return v + i;
        };
    });
};