'use strict';

console.log('Label', [1,2,3])
console.info('Label', [1,2,3])
Math.max(1,2,3)
Math.min(1,2,3)
setTimeout( x => x, 1200, 'Identity');
setInterval( x => x, 1200, 'Identity');
setImmediate( x => x,'Do it later');
